# Test environment overrides — copy to .env.test and fill in values
# Phase 1: integration tests (seed-parity, schema) apply migrations and run
# the seed against this database. Use a dedicated test DB, not the dev DB.
NODE_ENV=test
DATABASE_URL=postgresql://modars:modarsdev@localhost:5432/modars_test
REDIS_URL=redis://localhost:6379
CORS_ORIGINS=http://localhost:5173
PORT=4001
